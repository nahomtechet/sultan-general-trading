// next-sitemap.config.js
const config = {
  siteUrl: 'https://sultangeneraltrading.com', // Your domain name
  generateRobotsTxt: true, // Generate robots.txt as well
  sitemapSize: 7000, // Number of URLs per sitemap
  changefreq: 'daily', // Suggested frequency for search engines to crawl
  priority: 0.7, // Default priority for all pages
};

module.exports = config;
